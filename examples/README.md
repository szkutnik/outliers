# Examples

This folder contains runnable examples for the outlier utilities.

## Basic usage

```bash
python examples/basic_usage.py
```

The script prints the input values, modified z-scores, and which values are flagged as outliers.
